<UserControl x:Class="RTFTDIF.Client.UserControls.CategoryItemList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RTFTDIF.Client.UserControls"
             xmlns:vm ="clr-namespace:RTFTDIF.ViewModels;assembly=RTFTDIF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid DataContext="{x:Static vm:DCategoryItemListViewModel.Instance}" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="47*"/>
            <RowDefinition Height="178*"/>
        </Grid.RowDefinitions>
        <ScrollViewer Margin="0" Padding="0,0,0,0" Grid.RowSpan="2">
            <ItemsControl ItemsSource="{Binding List}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:CategoryItem></local:CategoryItem>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <!--<local:CategoryItem Height="100"></local:CategoryItem>-->
        </ScrollViewer>
    </Grid>
</UserControl>
